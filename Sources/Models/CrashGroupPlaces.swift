//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

public class CrashGroupPlaces: APIModel {

    public var crashCount: Int?

    public var places: [Places]?

    public init(crashCount: Int? = nil, places: [Places]? = nil) {
        self.crashCount = crashCount
        self.places = places
    }

    private enum CodingKeys: String, CodingKey {
        case crashCount = "crash_count"
        case places
    }

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        crashCount = try container.decodeIfPresent(.crashCount)
        places = try container.decodeArrayIfPresent(.places)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encodeIfPresent(crashCount, forKey: .crashCount)
        try container.encodeIfPresent(places, forKey: .places)
    }

    public func isEqual(to object: Any?) -> Bool {
      guard let object = object as? CrashGroupPlaces else { return false }
      guard self.crashCount == object.crashCount else { return false }
      guard self.places == object.places else { return false }
      return true
    }

    public static func == (lhs: CrashGroupPlaces, rhs: CrashGroupPlaces) -> Bool {
        return lhs.isEqual(to: rhs)
    }
}
