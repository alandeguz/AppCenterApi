//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

public class Language: APIModel {

    /** Count current of language. */
    public var count: Int?

    /** Language's name. */
    public var languageName: String?

    /** Count of previous lanugage. */
    public var previousCount: Int?

    public init(count: Int? = nil, languageName: String? = nil, previousCount: Int? = nil) {
        self.count = count
        self.languageName = languageName
        self.previousCount = previousCount
    }

    private enum CodingKeys: String, CodingKey {
        case count
        case languageName = "language_name"
        case previousCount = "previous_count"
    }

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        count = try container.decodeIfPresent(.count)
        languageName = try container.decodeIfPresent(.languageName)
        previousCount = try container.decodeIfPresent(.previousCount)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encodeIfPresent(count, forKey: .count)
        try container.encodeIfPresent(languageName, forKey: .languageName)
        try container.encodeIfPresent(previousCount, forKey: .previousCount)
    }

    public func isEqual(to object: Any?) -> Bool {
      guard let object = object as? Language else { return false }
      guard self.count == object.count else { return false }
      guard self.languageName == object.languageName else { return false }
      guard self.previousCount == object.previousCount else { return false }
      return true
    }

    public static func == (lhs: Language, rhs: Language) -> Bool {
        return lhs.isEqual(to: rhs)
    }
}
