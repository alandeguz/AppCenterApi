//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

/** The name of the device set and the list of device IDs */
public class DeviceSetUpdate: APIModel {

    /** List of device IDs */
    public var devices: [String]

    /** The name of the device set */
    public var name: String

    public init(devices: [String], name: String) {
        self.devices = devices
        self.name = name
    }

    private enum CodingKeys: String, CodingKey {
        case devices
        case name
    }

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        devices = try container.decodeArray(.devices)
        name = try container.decode(.name)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encode(devices, forKey: .devices)
        try container.encode(name, forKey: .name)
    }

    public func isEqual(to object: Any?) -> Bool {
      guard let object = object as? DeviceSetUpdate else { return false }
      guard self.devices == object.devices else { return false }
      guard self.name == object.name else { return false }
      return true
    }

    public static func == (lhs: DeviceSetUpdate, rhs: DeviceSetUpdate) -> Bool {
        return lhs.isEqual(to: rhs)
    }
}
