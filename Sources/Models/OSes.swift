//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

public class OSes: APIModel {

    public var oses: [Oses]?

    public var total: Int?

    public init(oses: [Oses]? = nil, total: Int? = nil) {
        self.oses = oses
        self.total = total
    }

    private enum CodingKeys: String, CodingKey {
        case oses
        case total
    }

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        oses = try container.decodeArrayIfPresent(.oses)
        total = try container.decodeIfPresent(.total)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encodeIfPresent(oses, forKey: .oses)
        try container.encodeIfPresent(total, forKey: .total)
    }

    public func isEqual(to object: Any?) -> Bool {
      guard let object = object as? OSes else { return false }
      guard self.oses == object.oses else { return false }
      guard self.total == object.total else { return false }
      return true
    }

    public static func == (lhs: OSes, rhs: OSes) -> Bool {
        return lhs.isEqual(to: rhs)
    }
}
