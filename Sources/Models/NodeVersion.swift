//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

/** The Node version */
public class NodeVersion: APIModel {

    /** If the Node version is default for AppCenter */
    public var current: Bool?

    /** The version name */
    public var name: String?

    public init(current: Bool? = nil, name: String? = nil) {
        self.current = current
        self.name = name
    }

    private enum CodingKeys: String, CodingKey {
        case current
        case name
    }

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        current = try container.decodeIfPresent(.current)
        name = try container.decodeIfPresent(.name)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encodeIfPresent(current, forKey: .current)
        try container.encodeIfPresent(name, forKey: .name)
    }

    public func isEqual(to object: Any?) -> Bool {
      guard let object = object as? NodeVersion else { return false }
      guard self.current == object.current else { return false }
      guard self.name == object.name else { return false }
      return true
    }

    public static func == (lhs: NodeVersion, rhs: NodeVersion) -> Bool {
        return lhs.isEqual(to: rhs)
    }
}
