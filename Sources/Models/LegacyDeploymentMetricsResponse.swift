//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

public class LegacyDeploymentMetricsResponse: APIModel {

    /** Object containing a property named after each release label, which contains an object that contains that release's metrics. */
    public var metrics: [String: Metrics]?

    public init(metrics: [String: Metrics]? = nil) {
        self.metrics = metrics
    }

    private enum CodingKeys: String, CodingKey {
        case metrics
    }

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        metrics = try container.decodeIfPresent(.metrics)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encodeIfPresent(metrics, forKey: .metrics)
    }

    public func isEqual(to object: Any?) -> Bool {
      guard let object = object as? LegacyDeploymentMetricsResponse else { return false }
      guard self.metrics == object.metrics else { return false }
      return true
    }

    public static func == (lhs: LegacyDeploymentMetricsResponse, rhs: LegacyDeploymentMetricsResponse) -> Bool {
        return lhs.isEqual(to: rhs)
    }
}
