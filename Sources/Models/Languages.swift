//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

public class Languages: APIModel {

    public var languages: [Languages]?

    public var total: Int?

    public init(languages: [Languages]? = nil, total: Int? = nil) {
        self.languages = languages
        self.total = total
    }

    private enum CodingKeys: String, CodingKey {
        case languages
        case total
    }

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        languages = try container.decodeArrayIfPresent(.languages)
        total = try container.decodeIfPresent(.total)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encodeIfPresent(languages, forKey: .languages)
        try container.encodeIfPresent(total, forKey: .total)
    }

    public func isEqual(to object: Any?) -> Bool {
      guard let object = object as? Languages else { return false }
      guard self.languages == object.languages else { return false }
      guard self.total == object.total else { return false }
      return true
    }

    public static func == (lhs: Languages, rhs: Languages) -> Bool {
        return lhs.isEqual(to: rhs)
    }
}
