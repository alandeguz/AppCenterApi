//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

public class OrganizationInvitationSimpleDetailResponse: APIModel {

    /** The unique ID (UUID) of the invitation */
    public var id: ID

    /** The email address of the invited user */
    public var email: String

    /** The role assigned to the invited user */
    public var role: String

    public init(id: ID, email: String, role: String) {
        self.id = id
        self.email = email
        self.role = role
    }

    private enum CodingKeys: String, CodingKey {
        case id
        case email
        case role
    }

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        id = try container.decode(.id)
        email = try container.decode(.email)
        role = try container.decode(.role)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encode(id, forKey: .id)
        try container.encode(email, forKey: .email)
        try container.encode(role, forKey: .role)
    }

    public func isEqual(to object: Any?) -> Bool {
      guard let object = object as? OrganizationInvitationSimpleDetailResponse else { return false }
      guard self.id == object.id else { return false }
      guard self.email == object.email else { return false }
      guard self.role == object.role else { return false }
      return true
    }

    public static func == (lhs: OrganizationInvitationSimpleDetailResponse, rhs: OrganizationInvitationSimpleDetailResponse) -> Bool {
        return lhs.isEqual(to: rhs)
    }
}
