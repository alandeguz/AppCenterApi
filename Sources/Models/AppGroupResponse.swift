//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

public class AppGroupResponse: APIModel {

    /** The OS the app will be running on */
    public enum Os: String, Codable {
        case android = "Android"
        case iOS = "iOS"
        case macOS = "macOS"
        case tizen = "Tizen"
        case tvOS = "tvOS"
        case windows = "Windows"
        case linux = "Linux"
        case custom = "Custom"

        public static let cases: [Os] = [
          .android,
          .iOS,
          .macOS,
          .tizen,
          .tvOS,
          .windows,
          .linux,
          .custom,
        ]
    }

    /** The platform of the app */
    public enum Platform: String, Codable {
        case java = "Java"
        case objectiveCSwift = "Objective-C-Swift"
        case uwp = "UWP"
        case cordova = "Cordova"
        case reactNative = "React-Native"
        case unity = "Unity"
        case electron = "Electron"
        case xamarin = "Xamarin"
        case wpf = "WPF"
        case winForms = "WinForms"
        case unknown = "Unknown"
        case custom = "Custom"

        public static let cases: [Platform] = [
          .java,
          .objectiveCSwift,
          .uwp,
          .cordova,
          .reactNative,
          .unity,
          .electron,
          .xamarin,
          .wpf,
          .winForms,
          .unknown,
          .custom,
        ]
    }

    /** The unique ID (UUID) of the app */
    public var id: ID

    /** The unique ID (UUID) of the group that the app belongs to */
    public var groupId: ID

    /** The name of the app used in URLs */
    public var name: String

    /** The OS the app will be running on */
    public var os: Os

    /** The platform of the app */
    public var platform: Platform

    /** The display name of the app */
    public var displayName: String?

    /** A one-word descriptive release-type value that starts with a capital letter but is otherwise lowercase */
    public var releaseType: String?

    public init(id: ID, groupId: ID, name: String, os: Os, platform: Platform, displayName: String? = nil, releaseType: String? = nil) {
        self.id = id
        self.groupId = groupId
        self.name = name
        self.os = os
        self.platform = platform
        self.displayName = displayName
        self.releaseType = releaseType
    }

    private enum CodingKeys: String, CodingKey {
        case id
        case groupId = "group_id"
        case name
        case os
        case platform
        case displayName = "display_name"
        case releaseType = "release_type"
    }

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        id = try container.decode(.id)
        groupId = try container.decode(.groupId)
        name = try container.decode(.name)
        os = try container.decode(.os)
        platform = try container.decode(.platform)
        displayName = try container.decodeIfPresent(.displayName)
        releaseType = try container.decodeIfPresent(.releaseType)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encode(id, forKey: .id)
        try container.encode(groupId, forKey: .groupId)
        try container.encode(name, forKey: .name)
        try container.encode(os, forKey: .os)
        try container.encode(platform, forKey: .platform)
        try container.encodeIfPresent(displayName, forKey: .displayName)
        try container.encodeIfPresent(releaseType, forKey: .releaseType)
    }

    public func isEqual(to object: Any?) -> Bool {
      guard let object = object as? AppGroupResponse else { return false }
      guard self.id == object.id else { return false }
      guard self.groupId == object.groupId else { return false }
      guard self.name == object.name else { return false }
      guard self.os == object.os else { return false }
      guard self.platform == object.platform else { return false }
      guard self.displayName == object.displayName else { return false }
      guard self.releaseType == object.releaseType else { return false }
      return true
    }

    public static func == (lhs: AppGroupResponse, rhs: AppGroupResponse) -> Bool {
        return lhs.isEqual(to: rhs)
    }
}
