//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

public class DateTimeDownloadReleaseCounts: APIModel {

    /** Release counts per day. */
    public var counts: [Counts]?

    public var total: Int?

    public var unique: Int?

    public init(counts: [Counts]? = nil, total: Int? = nil, unique: Int? = nil) {
        self.counts = counts
        self.total = total
        self.unique = unique
    }

    private enum CodingKeys: String, CodingKey {
        case counts
        case total
        case unique
    }

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        counts = try container.decodeArrayIfPresent(.counts)
        total = try container.decodeIfPresent(.total)
        unique = try container.decodeIfPresent(.unique)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encodeIfPresent(counts, forKey: .counts)
        try container.encodeIfPresent(total, forKey: .total)
        try container.encodeIfPresent(unique, forKey: .unique)
    }

    public func isEqual(to object: Any?) -> Bool {
      guard let object = object as? DateTimeDownloadReleaseCounts else { return false }
      guard self.counts == object.counts else { return false }
      guard self.total == object.total else { return false }
      guard self.unique == object.unique else { return false }
      return true
    }

    public static func == (lhs: DateTimeDownloadReleaseCounts, rhs: DateTimeDownloadReleaseCounts) -> Bool {
        return lhs.isEqual(to: rhs)
    }
}
