//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

/** URL that can be used to check the status of the update devices operation and the updated profiles. */
public class UpdateDevicesResponse: APIModel {

    /** URL that can be used to check the status of the update devices operation. */
    public var statusURL: String

    public init(statusURL: String) {
        self.statusURL = statusURL
    }

    private enum CodingKeys: String, CodingKey {
        case statusURL = "status_url"
    }

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        statusURL = try container.decode(.statusURL)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encode(statusURL, forKey: .statusURL)
    }

    public func isEqual(to object: Any?) -> Bool {
      guard let object = object as? UpdateDevicesResponse else { return false }
      guard self.statusURL == object.statusURL else { return false }
      return true
    }

    public static func == (lhs: UpdateDevicesResponse, rhs: UpdateDevicesResponse) -> Bool {
        return lhs.isEqual(to: rhs)
    }
}
