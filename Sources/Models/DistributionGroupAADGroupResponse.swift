//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

public class DistributionGroupAADGroupResponse: APIModel {

    /** The AAD unique id (UUID) of the AAD group. */
    public var aadGroupId: ID?

    /** The display name of the AAD group */
    public var displayName: String?

    /** The distribution group of the AAD group */
    public var distributionGroupName: String?

    /** The internal unique id (UUID) of the AAD group. */
    public var id: ID?

    public var isAadGroup: Bool?

    public init(aadGroupId: ID? = nil, displayName: String? = nil, distributionGroupName: String? = nil, id: ID? = nil, isAadGroup: Bool? = nil) {
        self.aadGroupId = aadGroupId
        self.displayName = displayName
        self.distributionGroupName = distributionGroupName
        self.id = id
        self.isAadGroup = isAadGroup
    }

    private enum CodingKeys: String, CodingKey {
        case aadGroupId = "aad_group_id"
        case displayName = "display_name"
        case distributionGroupName = "distribution_group_name"
        case id
        case isAadGroup = "is_aad_group"
    }

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        aadGroupId = try container.decodeIfPresent(.aadGroupId)
        displayName = try container.decodeIfPresent(.displayName)
        distributionGroupName = try container.decodeIfPresent(.distributionGroupName)
        id = try container.decodeIfPresent(.id)
        isAadGroup = try container.decodeIfPresent(.isAadGroup)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encodeIfPresent(aadGroupId, forKey: .aadGroupId)
        try container.encodeIfPresent(displayName, forKey: .displayName)
        try container.encodeIfPresent(distributionGroupName, forKey: .distributionGroupName)
        try container.encodeIfPresent(id, forKey: .id)
        try container.encodeIfPresent(isAadGroup, forKey: .isAadGroup)
    }

    public func isEqual(to object: Any?) -> Bool {
      guard let object = object as? DistributionGroupAADGroupResponse else { return false }
      guard self.aadGroupId == object.aadGroupId else { return false }
      guard self.displayName == object.displayName else { return false }
      guard self.distributionGroupName == object.distributionGroupName else { return false }
      guard self.id == object.id else { return false }
      guard self.isAadGroup == object.isAadGroup else { return false }
      return true
    }

    public static func == (lhs: DistributionGroupAADGroupResponse, rhs: DistributionGroupAADGroupResponse) -> Bool {
        return lhs.isEqual(to: rhs)
    }
}
