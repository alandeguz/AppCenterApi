//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

/** Required explicit begin session log (a marker event for analytics service). */
public class HanledErrorLogDiagnostics: APIModel {

    /** Error ID.
 */
    public var errorId: ID

    public init(errorId: ID) {
        self.errorId = errorId
    }

    private enum CodingKeys: String, CodingKey {
        case errorId = "error_id"
    }

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        errorId = try container.decode(.errorId)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encode(errorId, forKey: .errorId)
    }

    public func isEqual(to object: Any?) -> Bool {
      guard let object = object as? HanledErrorLogDiagnostics else { return false }
      guard self.errorId == object.errorId else { return false }
      return true
    }

    public static func == (lhs: HanledErrorLogDiagnostics, rhs: HanledErrorLogDiagnostics) -> Bool {
        return lhs.isEqual(to: rhs)
    }
}
