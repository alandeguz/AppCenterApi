//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

/** The response for the testflight metadata check. */
public class HasTestflightMetadataResponse: APIModel {

    /** true if the app has the testflight metadata, false otherwise */
    public var hasTestflightMetadata: Bool?

    public init(hasTestflightMetadata: Bool? = nil) {
        self.hasTestflightMetadata = hasTestflightMetadata
    }

    private enum CodingKeys: String, CodingKey {
        case hasTestflightMetadata = "has_testflight_metadata"
    }

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        hasTestflightMetadata = try container.decodeIfPresent(.hasTestflightMetadata)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encodeIfPresent(hasTestflightMetadata, forKey: .hasTestflightMetadata)
    }

    public func isEqual(to object: Any?) -> Bool {
      guard let object = object as? HasTestflightMetadataResponse else { return false }
      guard self.hasTestflightMetadata == object.hasTestflightMetadata else { return false }
      return true
    }

    public static func == (lhs: HasTestflightMetadataResponse, rhs: HasTestflightMetadataResponse) -> Bool {
        return lhs.isEqual(to: rhs)
    }
}
