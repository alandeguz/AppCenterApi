//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

/** Apple credentials needed to log into the Apple Developer Portal */
public class AppleMutifactorLoginRequest: APIModel {

    /** The username for the Apple Developer account. */
    public var username: String

    /** This is the six digit OTP used for completing the multi-factor authentication */
    public var authcode: String

    public init(username: String, authcode: String) {
        self.username = username
        self.authcode = authcode
    }

    private enum CodingKeys: String, CodingKey {
        case username
        case authcode
    }

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        username = try container.decode(.username)
        authcode = try container.decode(.authcode)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encode(username, forKey: .username)
        try container.encode(authcode, forKey: .authcode)
    }

    public func isEqual(to object: Any?) -> Bool {
      guard let object = object as? AppleMutifactorLoginRequest else { return false }
      guard self.username == object.username else { return false }
      guard self.authcode == object.authcode else { return false }
      return true
    }

    public static func == (lhs: AppleMutifactorLoginRequest, rhs: AppleMutifactorLoginRequest) -> Bool {
        return lhs.isEqual(to: rhs)
    }
}
