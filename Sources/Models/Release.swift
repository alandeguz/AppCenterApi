//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

public class Release: APIModel {

    /** Release Id.
 */
    public var release: String

    public init(release: String) {
        self.release = release
    }

    private enum CodingKeys: String, CodingKey {
        case release
    }

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        release = try container.decode(.release)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encode(release, forKey: .release)
    }

    public func isEqual(to object: Any?) -> Bool {
      guard let object = object as? Release else { return false }
      guard self.release == object.release else { return false }
      return true
    }

    public static func == (lhs: Release, rhs: Release) -> Bool {
        return lhs.isEqual(to: rhs)
    }
}
