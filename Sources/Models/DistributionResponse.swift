//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

public class DistributionResponse: APIModel {

    /** Status of the Request */
    public var status: String?

    /** A unique ID of the upload */
    public var uploadId: String?

    public init(status: String? = nil, uploadId: String? = nil) {
        self.status = status
        self.uploadId = uploadId
    }

    private enum CodingKeys: String, CodingKey {
        case status
        case uploadId = "upload_id"
    }

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        status = try container.decodeIfPresent(.status)
        uploadId = try container.decodeIfPresent(.uploadId)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encodeIfPresent(status, forKey: .status)
        try container.encodeIfPresent(uploadId, forKey: .uploadId)
    }

    public func isEqual(to object: Any?) -> Bool {
      guard let object = object as? DistributionResponse else { return false }
      guard self.status == object.status else { return false }
      guard self.uploadId == object.uploadId else { return false }
      return true
    }

    public static func == (lhs: DistributionResponse, rhs: DistributionResponse) -> Bool {
        return lhs.isEqual(to: rhs)
    }
}
