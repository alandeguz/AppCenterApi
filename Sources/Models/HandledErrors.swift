//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

public class HandledErrors: APIModel {

    /** Errors list. */
    public var errors: [Errors]?

    public var nextLink: String?

    public init(errors: [Errors]? = nil, nextLink: String? = nil) {
        self.errors = errors
        self.nextLink = nextLink
    }

    private enum CodingKeys: String, CodingKey {
        case errors
        case nextLink
    }

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        errors = try container.decodeArrayIfPresent(.errors)
        nextLink = try container.decodeIfPresent(.nextLink)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encodeIfPresent(errors, forKey: .errors)
        try container.encodeIfPresent(nextLink, forKey: .nextLink)
    }

    public func isEqual(to object: Any?) -> Bool {
      guard let object = object as? HandledErrors else { return false }
      guard self.errors == object.errors else { return false }
      guard self.nextLink == object.nextLink else { return false }
      return true
    }

    public static func == (lhs: HandledErrors, rhs: HandledErrors) -> Bool {
        return lhs.isEqual(to: rhs)
    }
}
