//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

/** An object containing a UUID for an architecture for an iOS app. */
public class ArchIdentifier: APIModel {

    /** The architecture that the UUID belongs to, i.e. armv7 or arm64. */
    public var architecture: String

    /** The unique identifier. */
    public var uuid: ID

    public init(architecture: String, uuid: ID) {
        self.architecture = architecture
        self.uuid = uuid
    }

    private enum CodingKeys: String, CodingKey {
        case architecture
        case uuid
    }

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        architecture = try container.decode(.architecture)
        uuid = try container.decode(.uuid)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encode(architecture, forKey: .architecture)
        try container.encode(uuid, forKey: .uuid)
    }

    public func isEqual(to object: Any?) -> Bool {
      guard let object = object as? ArchIdentifier else { return false }
      guard self.architecture == object.architecture else { return false }
      guard self.uuid == object.uuid else { return false }
      return true
    }

    public static func == (lhs: ArchIdentifier, rhs: ArchIdentifier) -> Bool {
        return lhs.isEqual(to: rhs)
    }
}
