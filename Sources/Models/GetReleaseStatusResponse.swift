//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

public class GetReleaseStatusResponse: APIModel {

    /** The current upload status. */
    public enum UploadStatus: String, Codable {
        case uploadStarted = "uploadStarted"
        case uploadFinished = "uploadFinished"
        case readyToBePublished = "readyToBePublished"
        case malwareDetected = "malwareDetected"
        case error = "error"

        public static let cases: [UploadStatus] = [
          .uploadStarted,
          .uploadFinished,
          .readyToBePublished,
          .malwareDetected,
          .error,
        ]
    }

    /** The ID for the upload. */
    public var id: ID

    /** The current upload status. */
    public var uploadStatus: UploadStatus

    /** Details describing what went wrong processing the upload. Will only be set if status = 'error'. */
    public var errorDetails: String?

    /** The distinct ID of the release. Will only be set when the status = 'readyToBePublished'. */
    public var releaseDistinctId: Double?

    /** The URL of the release. Will only be set when the status = 'readyToBePublished'. */
    public var releaseURL: UNKNOWN_ANY?

    public init(id: ID, uploadStatus: UploadStatus, errorDetails: String? = nil, releaseDistinctId: Double? = nil, releaseURL: UNKNOWN_ANY? = nil) {
        self.id = id
        self.uploadStatus = uploadStatus
        self.errorDetails = errorDetails
        self.releaseDistinctId = releaseDistinctId
        self.releaseURL = releaseURL
    }

    private enum CodingKeys: String, CodingKey {
        case id
        case uploadStatus = "upload_status"
        case errorDetails = "error_details"
        case releaseDistinctId = "release_distinct_id"
        case releaseURL = "release_url"
    }

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        id = try container.decode(.id)
        uploadStatus = try container.decode(.uploadStatus)
        errorDetails = try container.decodeIfPresent(.errorDetails)
        releaseDistinctId = try container.decodeIfPresent(.releaseDistinctId)
        releaseURL = try container.decodeIfPresent(.releaseURL)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encode(id, forKey: .id)
        try container.encode(uploadStatus, forKey: .uploadStatus)
        try container.encodeIfPresent(errorDetails, forKey: .errorDetails)
        try container.encodeIfPresent(releaseDistinctId, forKey: .releaseDistinctId)
        try container.encodeIfPresent(releaseURL, forKey: .releaseURL)
    }

    public func isEqual(to object: Any?) -> Bool {
      guard let object = object as? GetReleaseStatusResponse else { return false }
      guard self.id == object.id else { return false }
      guard self.uploadStatus == object.uploadStatus else { return false }
      guard self.errorDetails == object.errorDetails else { return false }
      guard self.releaseDistinctId == object.releaseDistinctId else { return false }
      guard self.releaseURL == object.releaseURL else { return false }
      return true
    }

    public static func == (lhs: GetReleaseStatusResponse, rhs: GetReleaseStatusResponse) -> Bool {
        return lhs.isEqual(to: rhs)
    }
}
