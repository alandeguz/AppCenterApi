//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

public class AddUserAsRoleRequest: APIModel {

    /** The role of the user to be added */
    public enum Role: String, Codable {
        case admin = "admin"
        case collaborator = "collaborator"
        case member = "member"

        public static let cases: [Role] = [
          .admin,
          .collaborator,
          .member,
        ]
    }

    /** The role of the user to be added */
    public var role: Role?

    public init(role: Role? = nil) {
        self.role = role
    }

    private enum CodingKeys: String, CodingKey {
        case role
    }

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        role = try container.decodeIfPresent(.role)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encodeIfPresent(role, forKey: .role)
    }

    public func isEqual(to object: Any?) -> Bool {
      guard let object = object as? AddUserAsRoleRequest else { return false }
      guard self.role == object.role else { return false }
      return true
    }

    public static func == (lhs: AddUserAsRoleRequest, rhs: AddUserAsRoleRequest) -> Bool {
        return lhs.isEqual(to: rhs)
    }
}
