//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

/** Apple connection secrets */
public class AppleCredentialsSecretRequest: APIModel {

    /** apple secret details */
    public var data: DataType

    /** Apple connection secrets */
    public class DataType: APIModel {

        /** 6 digit auth code */
        public var authCode: String?

        /** password to connect to apple store. */
        public var password: String?

        /** username to connect to apple store. */
        public var username: String?

        public init(authCode: String? = nil, password: String? = nil, username: String? = nil) {
            self.authCode = authCode
            self.password = password
            self.username = username
        }

        private enum CodingKeys: String, CodingKey {
            case authCode
            case password
            case username
        }

        public required init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)

            authCode = try container.decodeIfPresent(.authCode)
            password = try container.decodeIfPresent(.password)
            username = try container.decodeIfPresent(.username)
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            try container.encodeIfPresent(authCode, forKey: .authCode)
            try container.encodeIfPresent(password, forKey: .password)
            try container.encodeIfPresent(username, forKey: .username)
        }

        public func isEqual(to object: Any?) -> Bool {
          guard let object = object as? DataType else { return false }
          guard self.authCode == object.authCode else { return false }
          guard self.password == object.password else { return false }
          guard self.username == object.username else { return false }
          return true
        }

        public static func == (lhs: DataType, rhs: DataType) -> Bool {
            return lhs.isEqual(to: rhs)
        }
    }

    public init(data: DataType) {
        self.data = data
    }

    private enum CodingKeys: String, CodingKey {
        case data
    }

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        data = try container.decode(.data)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encode(data, forKey: .data)
    }

    public func isEqual(to object: Any?) -> Bool {
      guard let object = object as? AppleCredentialsSecretRequest else { return false }
      guard self.data == object.data else { return false }
      return true
    }

    public static func == (lhs: AppleCredentialsSecretRequest, rhs: AppleCredentialsSecretRequest) -> Bool {
        return lhs.isEqual(to: rhs)
    }
}
