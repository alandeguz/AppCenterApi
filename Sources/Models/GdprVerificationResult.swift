//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

/** GDPR tests verification result */
public class GdprVerificationResult: APIModel {

    /** Optional error message if the verification failed. */
    public var message: String?

    /** Verification status. True means that the verification was successfull. */
    public var status: Bool?

    public init(message: String? = nil, status: Bool? = nil) {
        self.message = message
        self.status = status
    }

    private enum CodingKeys: String, CodingKey {
        case message
        case status
    }

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        message = try container.decodeIfPresent(.message)
        status = try container.decodeIfPresent(.status)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encodeIfPresent(message, forKey: .message)
        try container.encodeIfPresent(status, forKey: .status)
    }

    public func isEqual(to object: Any?) -> Bool {
      guard let object = object as? GdprVerificationResult else { return false }
      guard self.message == object.message else { return false }
      guard self.status == object.status else { return false }
      return true
    }

    public static func == (lhs: GdprVerificationResult, rhs: GdprVerificationResult) -> Bool {
        return lhs.isEqual(to: rhs)
    }
}
