//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

public class AddAppTesterRequest: APIModel {

    /** The user ID of the tester that needs to be added */
    public var userId: ID

    /** The ID of the release the user was added to */
    public var releaseId: Int

    public init(userId: ID, releaseId: Int) {
        self.userId = userId
        self.releaseId = releaseId
    }

    private enum CodingKeys: String, CodingKey {
        case userId = "user_id"
        case releaseId = "release_id"
    }

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        userId = try container.decode(.userId)
        releaseId = try container.decode(.releaseId)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encode(userId, forKey: .userId)
        try container.encode(releaseId, forKey: .releaseId)
    }

    public func isEqual(to object: Any?) -> Bool {
      guard let object = object as? AddAppTesterRequest else { return false }
      guard self.userId == object.userId else { return false }
      guard self.releaseId == object.releaseId else { return false }
      return true
    }

    public static func == (lhs: AddAppTesterRequest, rhs: AddAppTesterRequest) -> Bool {
        return lhs.isEqual(to: rhs)
    }
}
