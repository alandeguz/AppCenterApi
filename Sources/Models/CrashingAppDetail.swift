//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

public class CrashingAppDetail: APIModel {

    /** application identifier */
    public var appId: String?

    /** application version */
    public var appVersion: String?

    /** crash group identifier */
    public var crashGroupId: String?

    public init(appId: String? = nil, appVersion: String? = nil, crashGroupId: String? = nil) {
        self.appId = appId
        self.appVersion = appVersion
        self.crashGroupId = crashGroupId
    }

    private enum CodingKeys: String, CodingKey {
        case appId
        case appVersion
        case crashGroupId
    }

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        appId = try container.decodeIfPresent(.appId)
        appVersion = try container.decodeIfPresent(.appVersion)
        crashGroupId = try container.decodeIfPresent(.crashGroupId)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encodeIfPresent(appId, forKey: .appId)
        try container.encodeIfPresent(appVersion, forKey: .appVersion)
        try container.encodeIfPresent(crashGroupId, forKey: .crashGroupId)
    }

    public func isEqual(to object: Any?) -> Bool {
      guard let object = object as? CrashingAppDetail else { return false }
      guard self.appId == object.appId else { return false }
      guard self.appVersion == object.appVersion else { return false }
      guard self.crashGroupId == object.crashGroupId else { return false }
      return true
    }

    public static func == (lhs: CrashingAppDetail, rhs: CrashingAppDetail) -> Bool {
        return lhs.isEqual(to: rhs)
    }
}
