//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

/** Required explicit begin session log (a marker event for analytics service). */
public class StartSessionLog: APIModel {

    /** Session ID.
 */
    public var sessionId: ID

    public init(sessionId: ID) {
        self.sessionId = sessionId
    }

    private enum CodingKeys: String, CodingKey {
        case sessionId = "session_id"
    }

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        sessionId = try container.decode(.sessionId)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encode(sessionId, forKey: .sessionId)
    }

    public func isEqual(to object: Any?) -> Bool {
      guard let object = object as? StartSessionLog else { return false }
      guard self.sessionId == object.sessionId else { return false }
      return true
    }

    public static func == (lhs: StartSessionLog, rhs: StartSessionLog) -> Bool {
        return lhs.isEqual(to: rhs)
    }
}
