//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

/** Short ID for a list of device IDs */
public class DeviceSelection: APIModel {

    /** Identifier of the device selection */
    public var shortId: String

    public init(shortId: String) {
        self.shortId = shortId
    }

    private enum CodingKeys: String, CodingKey {
        case shortId
    }

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        shortId = try container.decode(.shortId)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encode(shortId, forKey: .shortId)
    }

    public func isEqual(to object: Any?) -> Bool {
      guard let object = object as? DeviceSelection else { return false }
      guard self.shortId == object.shortId else { return false }
      return true
    }

    public static func == (lhs: DeviceSelection, rhs: DeviceSelection) -> Bool {
        return lhs.isEqual(to: rhs)
    }
}
