//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

public class CrashGroupModel: APIModel {

    /** Count of model. */
    public var crashCount: Int?

    /** Model's name. */
    public var modelName: String?

    public init(crashCount: Int? = nil, modelName: String? = nil) {
        self.crashCount = crashCount
        self.modelName = modelName
    }

    private enum CodingKeys: String, CodingKey {
        case crashCount = "crash_count"
        case modelName = "model_name"
    }

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        crashCount = try container.decodeIfPresent(.crashCount)
        modelName = try container.decodeIfPresent(.modelName)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encodeIfPresent(crashCount, forKey: .crashCount)
        try container.encodeIfPresent(modelName, forKey: .modelName)
    }

    public func isEqual(to object: Any?) -> Bool {
      guard let object = object as? CrashGroupModel else { return false }
      guard self.crashCount == object.crashCount else { return false }
      guard self.modelName == object.modelName else { return false }
      return true
    }

    public static func == (lhs: CrashGroupModel, rhs: CrashGroupModel) -> Bool {
        return lhs.isEqual(to: rhs)
    }
}
