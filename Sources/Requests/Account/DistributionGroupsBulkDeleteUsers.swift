//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

extension EngagementAPI.Account {

    /**
    Delete testers from distribution group in an org
    */
    public enum DistributionGroupsBulkDeleteUsers {

        public static let service = APIService<Response>(id: "distributionGroups_bulkDeleteUsers", tag: "account", method: "POST", path: "/v0.1/orgs/{org_name}/distribution_groups/{distribution_group_name}/members/bulk_delete", hasBody: true, securityRequirement: SecurityRequirement(type: "APIToken", scope: ""))

        public final class Request: APIRequest<Response> {

            /** Delete testers from distribution group in an org */
            public class Members: APIModel {

                /** The list of emails of the users */
                public var userEmails: [String]?

                public init(userEmails: [String]? = nil) {
                    self.userEmails = userEmails
                }

                private enum CodingKeys: String, CodingKey {
                    case userEmails = "user_emails"
                }

                public required init(from decoder: Decoder) throws {
                    let container = try decoder.container(keyedBy: CodingKeys.self)

                    userEmails = try container.decodeArrayIfPresent(.userEmails)
                }

                public func encode(to encoder: Encoder) throws {
                    var container = encoder.container(keyedBy: CodingKeys.self)

                    try container.encodeIfPresent(userEmails, forKey: .userEmails)
                }

                public func isEqual(to object: Any?) -> Bool {
                  guard let object = object as? Members else { return false }
                  guard self.userEmails == object.userEmails else { return false }
                  return true
                }

                public static func == (lhs: Members, rhs: Members) -> Bool {
                    return lhs.isEqual(to: rhs)
                }
            }

            public struct Options {

                /** The organization's name */
                public var orgName: String

                /** The name of the distribution group */
                public var distributionGroupName: String

                public init(orgName: String, distributionGroupName: String) {
                    self.orgName = orgName
                    self.distributionGroupName = distributionGroupName
                }
            }

            public var options: Options

            public var members: Members

            public init(members: Members, options: Options) {
                self.members = members
                self.options = options
                super.init(service: DistributionGroupsBulkDeleteUsers.service) {
                    let jsonEncoder = JSONEncoder()
                    return try jsonEncoder.encode(members)
                }
            }

            /// convenience initialiser so an Option doesn't have to be created
            public convenience init(orgName: String, distributionGroupName: String, members: Members) {
                let options = Options(orgName: orgName, distributionGroupName: distributionGroupName)
                self.init(members: members, options: options)
            }

            public override var path: String {
                return super.path.replacingOccurrences(of: "{" + "orgName" + "}", with: "\(self.options.orgName)").replacingOccurrences(of: "{" + "distributionGroupName" + "}", with: "\(self.options.distributionGroupName)")
            }
        }

        public enum Response: APIResponseValue, CustomStringConvertible, CustomDebugStringConvertible {
            public typealias SuccessType = Void

            /** Success */
            case status204

            public var success: Void? {
                switch self {
                case .status204: return ()
                }
            }

            public var response: Any {
                switch self {
                default: return ()
                }
            }

            public var statusCode: Int {
                switch self {
                case .status204: return 204
                }
            }

            public var successful: Bool {
                switch self {
                case .status204: return true
                }
            }

            public init(statusCode: Int, data: Data, decoder: ResponseDecoder) throws {
                switch statusCode {
                case 204: self = .status204
                default: throw APIClientError.unexpectedStatusCode(statusCode: statusCode, data: data)
                }
            }

            public var description: String {
                return "\(statusCode) \(successful ? "success" : "failure")"
            }

            public var debugDescription: String {
                var string = description
                let responseString = "\(response)"
                if responseString != "()" {
                    string += "\n\(responseString)"
                }
                return string
            }
        }
    }
}
