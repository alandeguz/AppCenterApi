//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

extension EngagementAPI.Crash {

    /**
    Available for UWP apps only.

    Gets a specific crash for an app. Available for UWP apps only.
    */
    public enum CrashesGet {

        public static let service = APIService<Response>(id: "crashes_get", tag: "crash", method: "GET", path: "/v0.1/apps/{owner_name}/{app_name}/crash_groups/{crash_group_id}/crashes/{crash_id}", hasBody: false, securityRequirement: SecurityRequirement(type: "APIToken", scope: ""))

        public final class Request: APIRequest<Response> {

            public struct Options {

                /** id of a specific group */
                public var crashGroupId: String

                /** id of a specific crash */
                public var crashId: String

                /** true if the crash should include the raw crash report. Default is false */
                public var includeReport: Bool?

                /** true if the crash should include the custom log report. Default is false */
                public var includeLog: Bool?

                /** true if the crash should include in depth crash details */
                public var includeDetails: Bool?

                /** true if the crash should include the stacktrace information */
                public var includeStacktrace: Bool?

                /** true if the stacktrace should be only the relevant thread / exception. Default is false */
                public var groupingOnly: Bool?

                /** The name of the owner */
                public var ownerName: String

                /** The name of the application */
                public var appName: String

                public init(crashGroupId: String, crashId: String, includeReport: Bool? = nil, includeLog: Bool? = nil, includeDetails: Bool? = nil, includeStacktrace: Bool? = nil, groupingOnly: Bool? = nil, ownerName: String, appName: String) {
                    self.crashGroupId = crashGroupId
                    self.crashId = crashId
                    self.includeReport = includeReport
                    self.includeLog = includeLog
                    self.includeDetails = includeDetails
                    self.includeStacktrace = includeStacktrace
                    self.groupingOnly = groupingOnly
                    self.ownerName = ownerName
                    self.appName = appName
                }
            }

            public var options: Options

            public init(options: Options) {
                self.options = options
                super.init(service: CrashesGet.service)
            }

            /// convenience initialiser so an Option doesn't have to be created
            public convenience init(crashGroupId: String, crashId: String, includeReport: Bool? = nil, includeLog: Bool? = nil, includeDetails: Bool? = nil, includeStacktrace: Bool? = nil, groupingOnly: Bool? = nil, ownerName: String, appName: String) {
                let options = Options(crashGroupId: crashGroupId, crashId: crashId, includeReport: includeReport, includeLog: includeLog, includeDetails: includeDetails, includeStacktrace: includeStacktrace, groupingOnly: groupingOnly, ownerName: ownerName, appName: appName)
                self.init(options: options)
            }

            public override var path: String {
                return super.path.replacingOccurrences(of: "{" + "crashGroupId" + "}", with: "\(self.options.crashGroupId)").replacingOccurrences(of: "{" + "crashId" + "}", with: "\(self.options.crashId)").replacingOccurrences(of: "{" + "ownerName" + "}", with: "\(self.options.ownerName)").replacingOccurrences(of: "{" + "appName" + "}", with: "\(self.options.appName)")
            }

            public override var parameters: [String: Any] {
                var params: [String: Any] = [:]
                if let includeReport = options.includeReport {
                  params["include_report"] = includeReport
                }
                if let includeLog = options.includeLog {
                  params["include_log"] = includeLog
                }
                if let includeDetails = options.includeDetails {
                  params["include_details"] = includeDetails
                }
                if let includeStacktrace = options.includeStacktrace {
                  params["include_stacktrace"] = includeStacktrace
                }
                if let groupingOnly = options.groupingOnly {
                  params["grouping_only"] = groupingOnly
                }
                return params
            }
        }

        public enum Response: APIResponseValue, CustomStringConvertible, CustomDebugStringConvertible {

            /** Gets a specific crash for an app. Available for UWP apps only. */
            public class DefaultResponse: APIModel {

                public var message: String

                public init(message: String) {
                    self.message = message
                }

                private enum CodingKeys: String, CodingKey {
                    case message
                }

                public required init(from decoder: Decoder) throws {
                    let container = try decoder.container(keyedBy: CodingKeys.self)

                    message = try container.decode(.message)
                }

                public func encode(to encoder: Encoder) throws {
                    var container = encoder.container(keyedBy: CodingKeys.self)

                    try container.encode(message, forKey: .message)
                }

                public func isEqual(to object: Any?) -> Bool {
                  guard let object = object as? DefaultResponse else { return false }
                  guard self.message == object.message else { return false }
                  return true
                }

                public static func == (lhs: DefaultResponse, rhs: DefaultResponse) -> Bool {
                    return lhs.isEqual(to: rhs)
                }
            }
            public typealias SuccessType = Crash

            /** Created */
            case status201(Crash)

            /** Error */
            case defaultResponse(statusCode: Int, DefaultResponse)

            public var success: Crash? {
                switch self {
                case .status201(let response): return response
                default: return nil
                }
            }

            public var failure: DefaultResponse? {
                switch self {
                case .defaultResponse(_, let response): return response
                default: return nil
                }
            }

            /// either success or failure value. Success is anything in the 200..<300 status code range
            public var responseResult: APIResponseResult<Crash, DefaultResponse> {
                if let successValue = success {
                    return .success(successValue)
                } else if let failureValue = failure {
                    return .failure(failureValue)
                } else {
                    fatalError("Response does not have success or failure response")
                }
            }

            public var response: Any {
                switch self {
                case .status201(let response): return response
                case .defaultResponse(_, let response): return response
                }
            }

            public var statusCode: Int {
                switch self {
                case .status201: return 201
                case .defaultResponse(let statusCode, _): return statusCode
                }
            }

            public var successful: Bool {
                switch self {
                case .status201: return true
                case .defaultResponse: return false
                }
            }

            public init(statusCode: Int, data: Data, decoder: ResponseDecoder) throws {
                switch statusCode {
                case 201: self = try .status201(decoder.decode(Crash.self, from: data))
                default: self = try .defaultResponse(statusCode: statusCode, decoder.decode(DefaultResponse.self, from: data))
                }
            }

            public var description: String {
                return "\(statusCode) \(successful ? "success" : "failure")"
            }

            public var debugDescription: String {
                var string = description
                let responseString = "\(response)"
                if responseString != "()" {
                    string += "\n\(responseString)"
                }
                return string
            }
        }
    }
}
