//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

extension EngagementAPI.Analytics {

    /**
    Count of devices for an event by interval in the time range.
    */
    public enum AnalyticsEventDeviceCount {

        public static let service = APIService<Response>(id: "Analytics_EventDeviceCount", tag: "analytics", method: "GET", path: "/v0.1/apps/{owner_name}/{app_name}/analytics/events/{event_name}/device_count", hasBody: false, securityRequirement: SecurityRequirement(type: "APIToken", scope: ""))

        public final class Request: APIRequest<Response> {

            public struct Options {

                /** The id of the event. */
                public var eventName: String

                /** Start date time in data in ISO 8601 date time format. */
                public var start: DateTime

                /** Last date time in data in ISO 8601 date time format. */
                public var end: DateTime?

                public var versions: [String]?

                /** The name of the owner */
                public var ownerName: String

                /** The name of the application */
                public var appName: String

                public init(eventName: String, start: DateTime, end: DateTime? = nil, versions: [String]? = nil, ownerName: String, appName: String) {
                    self.eventName = eventName
                    self.start = start
                    self.end = end
                    self.versions = versions
                    self.ownerName = ownerName
                    self.appName = appName
                }
            }

            public var options: Options

            public init(options: Options) {
                self.options = options
                super.init(service: AnalyticsEventDeviceCount.service)
            }

            /// convenience initialiser so an Option doesn't have to be created
            public convenience init(eventName: String, start: DateTime, end: DateTime? = nil, versions: [String]? = nil, ownerName: String, appName: String) {
                let options = Options(eventName: eventName, start: start, end: end, versions: versions, ownerName: ownerName, appName: appName)
                self.init(options: options)
            }

            public override var path: String {
                return super.path.replacingOccurrences(of: "{" + "eventName" + "}", with: "\(self.options.eventName)").replacingOccurrences(of: "{" + "ownerName" + "}", with: "\(self.options.ownerName)").replacingOccurrences(of: "{" + "appName" + "}", with: "\(self.options.appName)")
            }

            public override var parameters: [String: Any] {
                var params: [String: Any] = [:]
                params["start"] = options.start.encode()
                if let end = options.end?.encode() {
                  params["end"] = end
                }
                if let versions = options.versions?.joined(separator: "|") {
                  params["versions"] = versions
                }
                return params
            }
        }

        public enum Response: APIResponseValue, CustomStringConvertible, CustomDebugStringConvertible {

            /** Count of devices for an event by interval in the time range. */
            public class Status200: APIModel {

                public var devicesCount: [DevicesCount]?

                public var previousTotalDevicesWithEvent: Int?

                public var totalDevices: Int?

                public var totalDevicesWithEvent: Int?

                public init(devicesCount: [DevicesCount]? = nil, previousTotalDevicesWithEvent: Int? = nil, totalDevices: Int? = nil, totalDevicesWithEvent: Int? = nil) {
                    self.devicesCount = devicesCount
                    self.previousTotalDevicesWithEvent = previousTotalDevicesWithEvent
                    self.totalDevices = totalDevices
                    self.totalDevicesWithEvent = totalDevicesWithEvent
                }

                private enum CodingKeys: String, CodingKey {
                    case devicesCount = "devices_count"
                    case previousTotalDevicesWithEvent = "previous_total_devices_with_event"
                    case totalDevices = "total_devices"
                    case totalDevicesWithEvent = "total_devices_with_event"
                }

                public required init(from decoder: Decoder) throws {
                    let container = try decoder.container(keyedBy: CodingKeys.self)

                    devicesCount = try container.decodeArrayIfPresent(.devicesCount)
                    previousTotalDevicesWithEvent = try container.decodeIfPresent(.previousTotalDevicesWithEvent)
                    totalDevices = try container.decodeIfPresent(.totalDevices)
                    totalDevicesWithEvent = try container.decodeIfPresent(.totalDevicesWithEvent)
                }

                public func encode(to encoder: Encoder) throws {
                    var container = encoder.container(keyedBy: CodingKeys.self)

                    try container.encodeIfPresent(devicesCount, forKey: .devicesCount)
                    try container.encodeIfPresent(previousTotalDevicesWithEvent, forKey: .previousTotalDevicesWithEvent)
                    try container.encodeIfPresent(totalDevices, forKey: .totalDevices)
                    try container.encodeIfPresent(totalDevicesWithEvent, forKey: .totalDevicesWithEvent)
                }

                public func isEqual(to object: Any?) -> Bool {
                  guard let object = object as? Status200 else { return false }
                  guard self.devicesCount == object.devicesCount else { return false }
                  guard self.previousTotalDevicesWithEvent == object.previousTotalDevicesWithEvent else { return false }
                  guard self.totalDevices == object.totalDevices else { return false }
                  guard self.totalDevicesWithEvent == object.totalDevicesWithEvent else { return false }
                  return true
                }

                public static func == (lhs: Status200, rhs: Status200) -> Bool {
                    return lhs.isEqual(to: rhs)
                }
            }

            /** Count of devices for an event by interval in the time range. */
            public class DefaultResponse: APIModel {

                public var error: ErrorType?

                /** Count of devices for an event by interval in the time range. */
                public class ErrorType: APIModel {

                    /** The status code return by the API. It can be 400 or 403 or 500. */
                    public enum Code: Int, Codable {
                        case _400 = 400
                        case _403 = 403
                        case _500 = 500

                        public static let cases: [Code] = [
                          ._400,
                          ._403,
                          ._500,
                        ]
                    }

                    /** The status code return by the API. It can be 400 or 403 or 500. */
                    public var code: Code?

                    /** The reason for the request failed. */
                    public var message: String?

                    public init(code: Code? = nil, message: String? = nil) {
                        self.code = code
                        self.message = message
                    }

                    private enum CodingKeys: String, CodingKey {
                        case code
                        case message
                    }

                    public required init(from decoder: Decoder) throws {
                        let container = try decoder.container(keyedBy: CodingKeys.self)

                        code = try container.decodeIfPresent(.code)
                        message = try container.decodeIfPresent(.message)
                    }

                    public func encode(to encoder: Encoder) throws {
                        var container = encoder.container(keyedBy: CodingKeys.self)

                        try container.encodeIfPresent(code, forKey: .code)
                        try container.encodeIfPresent(message, forKey: .message)
                    }

                    public func isEqual(to object: Any?) -> Bool {
                      guard let object = object as? ErrorType else { return false }
                      guard self.code == object.code else { return false }
                      guard self.message == object.message else { return false }
                      return true
                    }

                    public static func == (lhs: ErrorType, rhs: ErrorType) -> Bool {
                        return lhs.isEqual(to: rhs)
                    }
                }

                public init(error: ErrorType? = nil) {
                    self.error = error
                }

                private enum CodingKeys: String, CodingKey {
                    case error
                }

                public required init(from decoder: Decoder) throws {
                    let container = try decoder.container(keyedBy: CodingKeys.self)

                    error = try container.decodeIfPresent(.error)
                }

                public func encode(to encoder: Encoder) throws {
                    var container = encoder.container(keyedBy: CodingKeys.self)

                    try container.encodeIfPresent(error, forKey: .error)
                }

                public func isEqual(to object: Any?) -> Bool {
                  guard let object = object as? DefaultResponse else { return false }
                  guard self.error == object.error else { return false }
                  return true
                }

                public static func == (lhs: DefaultResponse, rhs: DefaultResponse) -> Bool {
                    return lhs.isEqual(to: rhs)
                }
            }
            public typealias SuccessType = Status200

            /** Count of devices for an event by interval in the time range. */
            case status200(Status200)

            /** Error code with reason. */
            case defaultResponse(statusCode: Int, DefaultResponse)

            public var success: Status200? {
                switch self {
                case .status200(let response): return response
                default: return nil
                }
            }

            public var failure: DefaultResponse? {
                switch self {
                case .defaultResponse(_, let response): return response
                default: return nil
                }
            }

            /// either success or failure value. Success is anything in the 200..<300 status code range
            public var responseResult: APIResponseResult<Status200, DefaultResponse> {
                if let successValue = success {
                    return .success(successValue)
                } else if let failureValue = failure {
                    return .failure(failureValue)
                } else {
                    fatalError("Response does not have success or failure response")
                }
            }

            public var response: Any {
                switch self {
                case .status200(let response): return response
                case .defaultResponse(_, let response): return response
                }
            }

            public var statusCode: Int {
                switch self {
                case .status200: return 200
                case .defaultResponse(let statusCode, _): return statusCode
                }
            }

            public var successful: Bool {
                switch self {
                case .status200: return true
                case .defaultResponse: return false
                }
            }

            public init(statusCode: Int, data: Data, decoder: ResponseDecoder) throws {
                switch statusCode {
                case 200: self = try .status200(decoder.decode(Status200.self, from: data))
                default: self = try .defaultResponse(statusCode: statusCode, decoder.decode(DefaultResponse.self, from: data))
                }
            }

            public var description: String {
                return "\(statusCode) \(successful ? "success" : "failure")"
            }

            public var debugDescription: String {
                var string = description
                let responseString = "\(response)"
                if responseString != "()" {
                    string += "\n\(responseString)"
                }
                return string
            }
        }
    }
}
